# LLM model identifiers — update here to change models project-wide

LLM_ANALYSIS = "gpt-4.1-mini"   # content analysis (analyze_content, generate_briefing)
LLM_ROUTER = "gpt-4.1-mini"     # intent routing (IntentRouterPort)
LLM_AGENT = "gpt-4.1"           # knowledge agent (tool calling, chat completions)
